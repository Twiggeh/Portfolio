{
	"dependencies": {
		"@cypress/code-coverage": "^3.9.2",
		"cors": "^2.8.5",
		"dotenv": "^8.2.0",
		"express": "^4.17.1",
		"express-validator": "^6.6.1",
		"mongoose": "^5.10.9",
		"morgan": "^1.10.0"
	},
	"scripts": {
		"debug": "echo NODE_ENV=development > .env && nyc --silent nodemon app",
		"start": "echo NODE_ENV=production > .env && node app"
	},
	"name": "server",
	"version": "1.0.0",
	"main": "app.js",
	"author": "Twiggeh",
	"type": "module",
	"license": "MIT",
	"devDependencies": {
		"eslint-plugin-security": "^1.4.0"
	}
}
